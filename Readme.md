# Cerra Treasury/Staking smart contracts

## Building cerra.io Treasury/Staking smart contracts
- In nix shell run `cabal run cerra-cli compile`
- Run `./build-scripts.sh` and see the result in `plutus` folder
